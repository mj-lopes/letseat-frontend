import * as React from "react";
import { Text } from "@mantine/core";
import { Header } from "./components/Header";

const App: React.FC = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default App;

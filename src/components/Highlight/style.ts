import { createStyles } from "@mantine/core";

const useStyles = createStyles((theme, params, getRef) => {
  return {
    highlight: {
      fontWeight: "bold",
      color: "#D92211",
    },
  };
});

export { useStyles };

import { createStyles } from "@mantine/core";

const useStyle = createStyles(() => {
  return {
    aspas: {
      marginTop: "-6rem",

      width: "clamp(32px, 32px + 3vw, 128px)",
    },
  };
});

export { useStyle };
